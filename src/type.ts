export type ImageSize = {
  width: number;
  height: number;
};

export interface ICompProps {
  style?: React.CSSProperties;
  className?: string;
}

export interface MyObject {
  [key: string]: any;
}
